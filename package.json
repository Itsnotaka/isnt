{
	"name": "create-isnt",
	"description": "🚀 A clean next.js template that utilize tailwind CSS + Iron Session for local cookie authentication. Isnt stands for Iron+Session+Nextjs+Typescript",
	"version": "1.1.1",
	"author": "Min Chun Fu <hi@kakaka.dev>",
	"scripts": {
		"build": "tsup src/cli.ts --minify",
		"clean": "rm -rf lib",
		"dev": "tsup src/cli.ts --watch",
		"prepublishOnly": "npm run clean && npm run build"
	},
	"bin": "dist/cli.js",
	"files": [
		"dist",
		"templates"
	],
	"dependencies": {
		"create-create-app": "^7.0.0"
	},
	"devDependencies": {
		"tsup": "^4.6.1",
		"typescript": "^4.2.3"
	},
	"license": "MIT"
}
