{
	"name": "create-isnt",
	"description": "🚀A template with ESLint, Prettier & TypeScript built in. Powered by esbuild.",
	"version": "1.0.0",
	"author": "Min Chun Fu <hi@kakaka.dev>",
	"scripts": {
		"build": "tsup src/cli.ts --minify",
		"clean": "rm -rf lib",
		"dev": "tsup src/cli.ts --watch",
		"prepublishOnly": "npm run clean && npm run build"
	},
	"bin": "dist/cli.js",
	"files": [
		"dist",
		"templates"
	],
	"dependencies": {
		"create-create-app": "^7.0.0"
	},
	"devDependencies": {
		"tsup": "^4.6.1",
		"typescript": "^4.2.3"
	},
	"license": "MIT"
}